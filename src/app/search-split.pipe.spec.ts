import { SearchSplitPipe } from './search-split.pipe';

describe('SearchSplitPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchSplitPipe();
    expect(pipe).toBeTruthy();
  });
});
